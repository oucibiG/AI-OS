# Cerebrum - AIOS Agent SDK
# Requirements for the Cerebrum package

# Core dependencies
openai>=1.0.0
anthropic>=0.3.0
groq>=0.2.0

# For memory and storage
python-dotenv>=1.0.0

# For async operations
aiohttp>=3.9.0
asyncio>=3.4.3

# For web interface (optional)
# Already included in browser, no pip package needed

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
